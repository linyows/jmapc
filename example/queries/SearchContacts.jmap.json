{
  "jmapcDoc": "SearchContacts lists the address books and searches one of them in the same request, so a contact picker can be filled in with one round trip.",

  "methodCalls": [
    ["AddressBook/get", {
      "jmapcComment": "Which address books exist. A picker needs this whether or not the search finds anything.",
      "ids": null,
      "properties": ["id", "name", "isDefault"]
    }, "books"],

    ["ContactCard/query", {
      "jmapcComment": "The cards matching the phrase, surname first.",
      "filter": {
        "operator": "AND",
        "conditions": [
          {"inAddressBook": "{{addressBookId}}"},
          {"text": "{{phrase}}"},
          {"kind": "individual"}
        ]
      },
      "sort": [
        {"property": "name/surname"},
        {"property": "name/given"}
      ],
      "limit": "{{limit}}"
    }, "search"],

    ["ContactCard/get", {
      "jmapcComment": "Only the parts a list view shows. A card holds far more than this.",
      "#ids": {"resultOf": "search", "name": "ContactCard/query", "path": "/ids"},
      "properties": ["id", "uid", "name", "emails", "phones", "organizations"]
    }, "fetch"]
  ]
}
