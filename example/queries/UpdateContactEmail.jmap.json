{
  "_doc": "UpdateContactEmail changes one address on a card and makes it the preferred one, sending the two values that changed rather than the whole card.",

  "methodCalls": [
    ["ContactCard/set", {
      "update": {
        "{{cardId}}": {
          "emails/{{emailKey}}/address": "{{address}}",
          "emails/{{emailKey}}/pref": 1
        }
      }
    }, "update"]
  ],

  "_returns": "update"
}
